# 쌀집 계산기 테스트

## 🔢 기본 입력 기능 테스트

- [x] **TC-001**: 숫자 입력 (0-9) - 숫자 버튼(1-9) 클릭 시 화면에 숫자 표시
- [x] **TC-002**: "0" 상태에서 숫자 입력 - 화면에 "0" 표시된 상태에서 숫자 입력 시 자동 교체
- [x] **TC-003**: "-0" 상태에서 숫자 입력 - +/- 버튼으로 "-0" 만든 후 숫자 입력 시 "-숫자" 형태로 변환
- [x] **TC-004**: 연산 후 두 번째 숫자 입력 - 첫 번째 숫자 입력 후 연산자 클릭, 두 번째 숫자 입력 시 화면에 표시
- [x] **TC-005**: 계산 결과 후 숫자 입력 - 계산 결과 후 숫자 입력 시 새로운 계산 시작
- [x] **TC-006**: 소수점 입력 - 정수 입력 후 소수점 버튼 클릭, 숫자 입력 시 "정수.소수" 형태로 표시
- [x] **TC-007**: 소수점 중복 방지 - 이미 소수점이 있는 숫자에 소수점 추가 시도 시 추가되지 않음

---

## 🧮 사칙연산 기능 테스트

- [x] **TC-008**: 덧셈 연산 - `10 + 5 =` 수행 시 화면에 "15" 표시
- [x] **TC-009**: 뺄셈 연산 - `10 - 3 =` 수행 시 화면에 "7" 표시
- [x] **TC-010**: 곱셈 연산 - `6 * 7 =` 수행 시 화면에 "42" 표시
- [x] **TC-011**: 나눗셈 연산 - `20 / 4 =` 수행 시 화면에 "5" 표시
- [x] **TC-012**: 0으로 나누기 에러 - `10 / 0 =` 수행 시 "ERR" 표시, 이후 입력/연산 차단
- [x] **TC-013**: 연속 연산 (자동 계산) - `1 + 2 + 3` 입력 시 자동 계산되어 최종 결과 "6" 표시
- [x] **TC-014**: 등호 후 연산자 입력 - `10 + 5 =` 후 `* 2 =` 수행 시 "30" 표시
- [x] **TC-015**: 소수점 연산 - `3.5 + 2.5 =` 수행 시 "6" 표시 (불필요한 0 제거)

---

## 🔧 편의 기능 테스트

- [x] **TC-016**: 부호 변경 (양수 → 음수) - 숫자 입력 후 +/- 버튼 클릭 시 "-숫자" 표시
- [x] **TC-017**: 부호 변경 (음수 → 양수) - 음수 상태에서 +/- 버튼 다시 클릭 시 양수로 변환
- [x] **TC-018**: 계산 중 부호 변경 - 계산 중 두 번째 숫자 입력 후 +/- 버튼 클릭 시 부호 변경
- [x] **TC-019**: CE 버튼 (계산 중이 아닐 때) - 숫자 입력 후 CE 버튼 클릭 시 첫 번째 숫자만 "0"으로 초기화
- [x] **TC-020**: CE 버튼 (계산 중일 때) - 계산 중 CE 버튼 클릭 시 두 번째 숫자만 "0"으로 초기화, 첫 번째 숫자와 연산자 유지
- [x] **TC-021**: AC 버튼 (전체 초기화) - AC 버튼 클릭 시 모든 입력값과 연산 초기화, 화면에 "0" 표시
- [x] **TC-022**: AC 버튼 (에러 상태 해제) - 에러 상태에서 AC 버튼 클릭 시 에러 해제, 정상 동작 가능

---

## ⚠️ 에러 처리 테스트

- [x] **TC-023**: 에러 상태에서 입력 차단 - "ERR" 표시 후 숫자 버튼 클릭 시 입력 차단, "ERR" 상태 유지
- [x] **TC-024**: 에러 상태에서 연산 차단 - "ERR" 표시 후 연산자 버튼 클릭 시 연산 차단, "ERR" 상태 유지

---

## ✨ 계산 정확도 테스트

- [x] **TC-025**: 소수점 계산 정확도 - `0.1 + 0.2` 계산 시 정확한 결과 "0.3" 표시 (부동소수점 오차 없음)
- [x] **TC-026**: 결과 포맷팅 (불필요한 0 제거) - `1.0 + 0.0` 계산 시 "1" 표시, `1.50 + 0.0` 계산 시 "1.5" 표시

---

## 💡 사용 팁 테스트

- [ ] **TC-027**: 연속 계산 - `10 + 5 =` 후 `* 2 =`, `- 10 =` 수행 시 연속 계산되어 최종 결과 "20" 표시
- [ ] **TC-028**: 새로운 계산 시작 - `10 + 5 =` 후 숫자 입력 시 이전 계산 결과 무시하고 새 계산 시작

---
